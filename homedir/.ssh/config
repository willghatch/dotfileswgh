# number of ignorable "are you alive?" pings
ServerAliveCountMax 3
# number of seconds of inactivity before pinging
ServerAliveInterval 300
# just check the fingerprint, don't check that the IP is also known
CheckHostIP no

include ~/dotfileswgh-dotlocal/ssh.conf
include ~/dotfileswgh-pri/ssh.conf

