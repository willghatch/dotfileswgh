# Git Squashing

Never squash, merge, or amend commits unless EXPLICITLY told to do so by the user.
Even if explicitly told to do so, it is only an instruction to do so ONCE, not again in any follow-up work without another explicit instruction to do so.
