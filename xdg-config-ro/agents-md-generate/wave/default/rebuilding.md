# Rebuilding Wave Components

The `waveasm` and `water` directories contain C++ and must be rebuilt after edits.
Run `WAVE_DIR=$PWD $WAVE_DIR/build_tools/wave-dev-setup.sh --build-waveasm` or `--build-water` from the worktree root.
