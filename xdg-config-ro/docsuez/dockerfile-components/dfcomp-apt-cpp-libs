# C++ standard library development packages (without build-essential)
RUN DEBIAN_FRONTEND=noninteractive apt install -y libstdc++-14-dev
RUN DEBIAN_FRONTEND=noninteractive apt install -y libstdc++-13-dev
