
# At one point I wanted to have a global xinitrc file, then source local options,
# but for now I think I'll just have a template that I can modify... If it doesn't
# end up being a bother syncing because I just use different options on different
# machines (which has somewhat been the case so far), then it won't matter.

# merge xresources first, so other programs in this (xscreensaver) can get settings
xrdb $DOTFILESWGH/Xresources

# Unclutter hides the mouse pointer after the given number of seconds
unclutter -idle 5 &

xscreensaver -no-splash &
stopTouchpadClick.sh &
hkk # get keyboard layout
#xmodmap ~/.Xmodmap
feh --bg-max ~/wallpaper
tic $DOTFILESWGH/xterm-256color-italic.terminfo

# good trayer command
#trayer --edge top --align right --width 10 --height 12 --transparent true --alpha 255 &
xcompmgr &
nm-applet &
$DOTFILESWGH/external/misc/libnotifylog/libnotifylogger.py $HOME/.cache/notifications.log &

exec xmonad

